{
    "name": "Intent Agent",
    "version": "1.0",
    "description": "An AI agent specialized in analyzing user input to determine and manage intents",
    "intents": {
      "analyze_intent": {
        "description": "Analyze user input to determine the most appropriate intent",
        "examples": [
          "I want to book a flight",
          "How do I reset my password?",
          "What's the weather like today?"
        ]
      },
      "create_intent": {
        "description": "Create a new intent based on user input",
        "examples": [
          "Create a new intent for booking appointments",
          "Add an intent for product inquiries",
          "Set up a new intent for customer support requests"
        ]
      },
      "update_intent": {
        "description": "Update an existing intent",
        "examples": [
          "Update the description of the 'bookFlight' intent",
          "Add new keywords to the 'resetPassword' intent",
          "Modify the 'checkWeather' intent"
        ]
      },
      "delete_intent": {
        "description": "Delete an existing intent",
        "examples": [
          "Remove the 'outdatedFeature' intent",
          "Delete the intent for 'legacySupport'",
          "Erase the 'deprecatedFunction' intent"
        ]
      }
    },
    "functions": [
      {
        "name": "process_task",
        "description": "Process a task to determine or manage an intent",
        "parameters": {
          "type": "object",
          "properties": {
            "user_input": {
              "type": "string",
              "description": "The user's input message"
            },
            "task": {
              "type": "string",
              "description": "The specific task to perform"
            },
            "context": {
              "type": "object",
              "description": "Additional context for the task"
            }
          },
          "required": ["user_input", "task", "context"]
        }
      },
      {
        "name": "analyze_intent",
        "description": "Analyze user input to determine the most appropriate intent",
        "parameters": {
          "type": "object",
          "properties": {
            "user_input": {
              "type": "string",
              "description": "The user's input message"
            },
            "context": {
              "type": "object",
              "description": "Additional context for the analysis"
            },
            "all_intents": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "name": {"type": "string"},
                  "description": {"type": "string"},
                  "keywords": {"type": "array", "items": {"type": "string"}}
                }
              },
              "description": "List of all existing intents"
            }
          },
          "required": ["user_input", "context", "all_intents"]
        }
      },
      {
        "name": "create_intent",
        "description": "Create a new intent",
        "parameters": {
          "type": "object",
          "properties": {
            "data": {
              "type": "object",
              "properties": {
                "name": {"type": "string"},
                "description": {"type": "string"},
                "keywords": {"type": "array", "items": {"type": "string"}}
              },
              "required": ["name", "description"]
            }
          },
          "required": ["data"]
        }
      },
      {
        "name": "get_intent",
        "description": "Retrieve details of a specific intent",
        "parameters": {
          "type": "object",
          "properties": {
            "intent_id": {
              "type": "string",
              "description": "The ID of the intent to retrieve"
            }
          },
          "required": ["intent_id"]
        }
      },
      {
        "name": "update_intent",
        "description": "Update an existing intent",
        "parameters": {
          "type": "object",
          "properties": {
            "intent_id": {
              "type": "string",
              "description": "The ID of the intent to update"
            },
            "data": {
              "type": "object",
              "properties": {
                "name": {"type": "string"},
                "description": {"type": "string"},
                "keywords": {"type": "array", "items": {"type": "string"}}
              }
            }
          },
          "required": ["intent_id", "data"]
        }
      },
      {
        "name": "delete_intent",
        "description": "Delete an existing intent",
        "parameters": {
          "type": "object",
          "properties": {
            "intent_id": {
              "type": "string",
              "description": "The ID of the intent to delete"
            }
          },
          "required": ["intent_id"]
        }
      }
    ],
    "system_instructions": "You are an Intent Agent specialized in analyzing user input to determine and manage intents. Your primary functions include identifying the most appropriate intent for a given user input, creating new intents when necessary, and managing existing intents. Always strive to match user input with existing intents when possible, and create specific, meaningful new intents when required. Be prepared to handle both global and user-specific intents, and ensure that your responses are clear and actionable. When analyzing intents, consider the context provided and use it to inform your decisions. Remember to use camelCase for intent names and provide relevant keywords for each intent."
  }